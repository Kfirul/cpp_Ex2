#include <string>

using namespace std;
namespace ariel{
    class card{
        public:

        int num;
        string suit;

        card(int num,String suit);

        int compare(card a);
        string toString();
        
    }
}

